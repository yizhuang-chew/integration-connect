import * as winston from 'winston';
import type { TLoggerOptions } from './types';
declare const createApplicationLogger: (options?: TLoggerOptions) => winston.Logger;
export default createApplicationLogger;
